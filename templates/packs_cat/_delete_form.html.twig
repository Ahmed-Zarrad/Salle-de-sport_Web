<form method="post" action="{{ path('packs_cat_delete', {'id': packs_cat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ packs_cat.id) }}">
    <button class="btn">Delete</button>
</form>
