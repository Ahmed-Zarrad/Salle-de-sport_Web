{% extends 'baseBack-Office.html.twig' %}

{% block title %}Packs Category{% endblock %}

{% block body %}
<div class="wrapper">
    <div class="sa4d25">
        <div class="container" style="margin-left: 5%">
    <h1>Packs Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ packs_cat.id }}</td>
            </tr>
            <tr>
                <th>Pcat_nom</th>
                <td>{{ packs_cat.pcatNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('packs_cat_index') }}">back to list</a>

    <a href="{{ path('packs_cat_edit', {'id': packs_cat.id}) }}">edit</a>

    {{ include('packs_cat/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
